html, body {
    height: 100%; /* Ensures full-height layout */
}

body {
    font-family: Arial, Helvetica, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    min-height: -webkit-fill-available; /* Fallback for iOS Safari */
}

header {
    background-color: #333;
    color: white;
    padding: 1rem;
    text-align: center;
}

main {
    padding: 1rem;
    flex: 1; /* Flexbox grow to fill available space */
}

footer {
    background-color: #333;
    color: white;
    padding: 1rem;
    text-align: center;
    width: 100%;
}

@media (max-width: 600px) {
    header, footer {
        padding: 0.5rem;
    }

    main {
        padding: 0.5rem;
    }
}

@media (min-width: 768px) {
    header, footer {
        padding: 2rem;
    }

    main {
        padding: 2rem;
    }
}
