* {
    box-sizing: border-box; /* Helps manage sizing of elements */
}

html, body {
    height: 100%; /* Ensures full-height layout */
}

body {
    font-family: Arial, Helvetica, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    min-height: -webkit-fill-available; /* Fallback for iOS Safari */
}

header, footer {
    background-color: #333;
    color: white;
    padding: 1rem;
    text-align: center;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Added shadow for depth */
}

main {
    padding: 1rem;
    flex: 1; /* Flexbox grow to fill available space */
}

@media (max-width: 600px) {
    header, footer, main {
        padding: 0.5rem; /* Adjust padding for small screens */
    }
}

@media (min-width: 768px) {
    header, footer, main {
        padding: 2rem; /* Adjust padding for larger screens */
    }
}
